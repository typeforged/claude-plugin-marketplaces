import { z } from "zod"

export default z.object({ "$schema": z.string().describe("JSON Schema reference for editor autocomplete/validation; ignored at load time").optional(), "name": z.string().min(1), "version": z.string().describe("Marketplace manifest version").optional(), "description": z.string().describe("Human-readable description of this marketplace").optional(), "owner": z.object({ "name": z.string().min(1).describe("Display name of the plugin author or organization"), "email": z.string().describe("Contact email for support or feedback").optional(), "url": z.string().describe("Website, GitHub profile, or organization URL").optional() }).describe("Marketplace maintainer or curator information"), "plugins": z.array(z.object({ "$schema": z.string().describe("JSON Schema reference for editor autocomplete/validation; ignored at load time").optional(), "name": z.string().min(1).describe("Unique identifier matching the plugin name"), "version": z.string().describe("Semantic version (e.g., 1.2.3) following semver.org specification").optional(), "description": z.string().describe("Brief, user-facing explanation of what the plugin provides").optional(), "author": z.object({ "name": z.string().min(1).describe("Display name of the plugin author or organization"), "email": z.string().describe("Contact email for support or feedback").optional(), "url": z.string().describe("Website, GitHub profile, or organization URL").optional() }).describe("Information about the plugin creator or maintainer").optional(), "homepage": z.string().url().describe("Plugin homepage or documentation URL").optional(), "repository": z.string().describe("Source code repository URL").optional(), "license": z.string().describe("SPDX license identifier (e.g., MIT, Apache-2.0)").optional(), "keywords": z.array(z.string()).describe("Tags for plugin discovery and categorization").optional(), "dependencies": z.array(z.union([z.string().regex(new RegExp("^[A-Za-z0-9][-A-Za-z0-9._]*(@[A-Za-z0-9][-A-Za-z0-9._]*)?(@\\^[^@]*)?$")), z.object({ "name": z.string().regex(new RegExp("^[A-Za-z0-9][-A-Za-z0-9._]*$")).min(1), "marketplace": z.string().regex(new RegExp("^[A-Za-z0-9][-A-Za-z0-9._]*$")).min(1).optional() }).catchall(z.any())])).describe("Plugins that must be enabled for this plugin to function. Bare names (no \"@marketplace\") are resolved against the declaring plugin's own marketplace.").optional(), "hooks": z.union([z.intersection(z.string().regex(new RegExp("^\\.\\/.*")), z.string().regex(new RegExp(".*\\.json$"))).describe("Path to file with additional hooks (in addition to those in hooks/hooks.json, if it exists), relative to the plugin root"), z.record(z.string(), z.array(z.object({ "matcher": z.string().describe("String pattern to match (e.g. tool names like \"Write\")").optional(), "hooks": z.array(z.union([z.object({ "type": z.literal("command").describe("Shell command hook type"), "command": z.string().describe("Shell command to execute"), "if": z.string().describe("Permission rule syntax to filter when this hook runs (e.g., \"Bash(git *)\"). Only runs if the tool call matches the pattern. Avoids spawning hooks for non-matching commands.").optional(), "shell": z.enum(["bash", "powershell"]).describe("Shell interpreter. 'bash' uses your $SHELL (bash/zsh/sh); 'powershell' uses pwsh. Defaults to bash.").optional(), "timeout": z.number().gt(0).describe("Timeout in seconds for this specific command").optional(), "statusMessage": z.string().describe("Custom status message to display in spinner while hook runs").optional(), "once": z.boolean().describe("If true, hook runs once and is removed after execution").optional(), "async": z.boolean().describe("If true, hook runs in background without blocking").optional(), "asyncRewake": z.boolean().describe("If true, hook runs in background and wakes the model on exit code 2 (blocking error). Implies async.").optional() }), z.object({ "type": z.literal("prompt").describe("LLM prompt hook type"), "prompt": z.string().describe("Prompt to evaluate with LLM. Use $ARGUMENTS placeholder for hook input JSON."), "if": z.string().describe("Permission rule syntax to filter when this hook runs (e.g., \"Bash(git *)\"). Only runs if the tool call matches the pattern. Avoids spawning hooks for non-matching commands.").optional(), "timeout": z.number().gt(0).describe("Timeout in seconds for this specific prompt evaluation").optional(), "model": z.string().describe("Model to use for this prompt hook (e.g., \"claude-sonnet-4-6\"). If not specified, uses the default small fast model.").optional(), "statusMessage": z.string().describe("Custom status message to display in spinner while hook runs").optional(), "once": z.boolean().describe("If true, hook runs once and is removed after execution").optional() }), z.object({ "type": z.literal("agent").describe("Agentic verifier hook type"), "prompt": z.string().describe("Prompt describing what to verify (e.g. \"Verify that unit tests ran and passed.\"). Use $ARGUMENTS placeholder for hook input JSON."), "if": z.string().describe("Permission rule syntax to filter when this hook runs (e.g., \"Bash(git *)\"). Only runs if the tool call matches the pattern. Avoids spawning hooks for non-matching commands.").optional(), "timeout": z.number().gt(0).describe("Timeout in seconds for agent execution (default 60)").optional(), "model": z.string().describe("Model to use for this agent hook (e.g., \"claude-sonnet-4-6\"). If not specified, uses Haiku.").optional(), "statusMessage": z.string().describe("Custom status message to display in spinner while hook runs").optional(), "once": z.boolean().describe("If true, hook runs once and is removed after execution").optional() }), z.object({ "type": z.literal("http").describe("HTTP hook type"), "url": z.string().url().describe("URL to POST the hook input JSON to"), "if": z.string().describe("Permission rule syntax to filter when this hook runs (e.g., \"Bash(git *)\"). Only runs if the tool call matches the pattern. Avoids spawning hooks for non-matching commands.").optional(), "timeout": z.number().gt(0).describe("Timeout in seconds for this specific request").optional(), "headers": z.record(z.string(), z.string()).describe("Additional headers to include in the request. Values may reference environment variables using $VAR_NAME or ${VAR_NAME} syntax (e.g., \"Authorization\": \"Bearer $MY_TOKEN\"). Only variables listed in allowedEnvVars will be interpolated.").optional(), "allowedEnvVars": z.array(z.string()).describe("Explicit list of environment variable names that may be interpolated in header values. Only variables listed here will be resolved; all other $VAR references are left as empty strings. Required for env var interpolation to work.").optional(), "statusMessage": z.string().describe("Custom status message to display in spinner while hook runs").optional(), "once": z.boolean().describe("If true, hook runs once and is removed after execution").optional() }), z.object({ "type": z.literal("mcp_tool").describe("MCP tool hook type"), "server": z.string().describe("Name of an already-configured MCP server to invoke"), "tool": z.string().describe("Name of the tool on that server to call"), "input": z.record(z.string(), z.any()).describe("Arguments passed to the MCP tool. String values support ${path} interpolation from the hook input JSON (e.g. \"${tool_input.file_path}\").").optional(), "if": z.string().describe("Permission rule syntax to filter when this hook runs (e.g., \"Bash(git *)\"). Only runs if the tool call matches the pattern. Avoids spawning hooks for non-matching commands.").optional(), "timeout": z.number().gt(0).describe("Timeout in seconds for this specific tool call").optional(), "statusMessage": z.string().describe("Custom status message to display in spinner while hook runs").optional(), "once": z.boolean().describe("If true, hook runs once and is removed after execution").optional() })])).describe("List of hooks to execute when the matcher matches") }))).describe("Additional hooks (in addition to those in hooks/hooks.json, if it exists)"), z.array(z.union([z.intersection(z.string().regex(new RegExp("^\\.\\/.*")), z.string().regex(new RegExp(".*\\.json$"))).describe("Path to file with additional hooks (in addition to those in hooks/hooks.json, if it exists), relative to the plugin root"), z.record(z.string(), z.array(z.object({ "matcher": z.string().describe("String pattern to match (e.g. tool names like \"Write\")").optional(), "hooks": z.array(z.union([z.object({ "type": z.literal("command").describe("Shell command hook type"), "command": z.string().describe("Shell command to execute"), "if": z.string().describe("Permission rule syntax to filter when this hook runs (e.g., \"Bash(git *)\"). Only runs if the tool call matches the pattern. Avoids spawning hooks for non-matching commands.").optional(), "shell": z.enum(["bash", "powershell"]).describe("Shell interpreter. 'bash' uses your $SHELL (bash/zsh/sh); 'powershell' uses pwsh. Defaults to bash.").optional(), "timeout": z.number().gt(0).describe("Timeout in seconds for this specific command").optional(), "statusMessage": z.string().describe("Custom status message to display in spinner while hook runs").optional(), "once": z.boolean().describe("If true, hook runs once and is removed after execution").optional(), "async": z.boolean().describe("If true, hook runs in background without blocking").optional(), "asyncRewake": z.boolean().describe("If true, hook runs in background and wakes the model on exit code 2 (blocking error). Implies async.").optional() }), z.object({ "type": z.literal("prompt").describe("LLM prompt hook type"), "prompt": z.string().describe("Prompt to evaluate with LLM. Use $ARGUMENTS placeholder for hook input JSON."), "if": z.string().describe("Permission rule syntax to filter when this hook runs (e.g., \"Bash(git *)\"). Only runs if the tool call matches the pattern. Avoids spawning hooks for non-matching commands.").optional(), "timeout": z.number().gt(0).describe("Timeout in seconds for this specific prompt evaluation").optional(), "model": z.string().describe("Model to use for this prompt hook (e.g., \"claude-sonnet-4-6\"). If not specified, uses the default small fast model.").optional(), "statusMessage": z.string().describe("Custom status message to display in spinner while hook runs").optional(), "once": z.boolean().describe("If true, hook runs once and is removed after execution").optional() }), z.object({ "type": z.literal("agent").describe("Agentic verifier hook type"), "prompt": z.string().describe("Prompt describing what to verify (e.g. \"Verify that unit tests ran and passed.\"). Use $ARGUMENTS placeholder for hook input JSON."), "if": z.string().describe("Permission rule syntax to filter when this hook runs (e.g., \"Bash(git *)\"). Only runs if the tool call matches the pattern. Avoids spawning hooks for non-matching commands.").optional(), "timeout": z.number().gt(0).describe("Timeout in seconds for agent execution (default 60)").optional(), "model": z.string().describe("Model to use for this agent hook (e.g., \"claude-sonnet-4-6\"). If not specified, uses Haiku.").optional(), "statusMessage": z.string().describe("Custom status message to display in spinner while hook runs").optional(), "once": z.boolean().describe("If true, hook runs once and is removed after execution").optional() }), z.object({ "type": z.literal("http").describe("HTTP hook type"), "url": z.string().url().describe("URL to POST the hook input JSON to"), "if": z.string().describe("Permission rule syntax to filter when this hook runs (e.g., \"Bash(git *)\"). Only runs if the tool call matches the pattern. Avoids spawning hooks for non-matching commands.").optional(), "timeout": z.number().gt(0).describe("Timeout in seconds for this specific request").optional(), "headers": z.record(z.string(), z.string()).describe("Additional headers to include in the request. Values may reference environment variables using $VAR_NAME or ${VAR_NAME} syntax (e.g., \"Authorization\": \"Bearer $MY_TOKEN\"). Only variables listed in allowedEnvVars will be interpolated.").optional(), "allowedEnvVars": z.array(z.string()).describe("Explicit list of environment variable names that may be interpolated in header values. Only variables listed here will be resolved; all other $VAR references are left as empty strings. Required for env var interpolation to work.").optional(), "statusMessage": z.string().describe("Custom status message to display in spinner while hook runs").optional(), "once": z.boolean().describe("If true, hook runs once and is removed after execution").optional() }), z.object({ "type": z.literal("mcp_tool").describe("MCP tool hook type"), "server": z.string().describe("Name of an already-configured MCP server to invoke"), "tool": z.string().describe("Name of the tool on that server to call"), "input": z.record(z.string(), z.any()).describe("Arguments passed to the MCP tool. String values support ${path} interpolation from the hook input JSON (e.g. \"${tool_input.file_path}\").").optional(), "if": z.string().describe("Permission rule syntax to filter when this hook runs (e.g., \"Bash(git *)\"). Only runs if the tool call matches the pattern. Avoids spawning hooks for non-matching commands.").optional(), "timeout": z.number().gt(0).describe("Timeout in seconds for this specific tool call").optional(), "statusMessage": z.string().describe("Custom status message to display in spinner while hook runs").optional(), "once": z.boolean().describe("If true, hook runs once and is removed after execution").optional() })])).describe("List of hooks to execute when the matcher matches") }))).describe("Additional hooks (in addition to those in hooks/hooks.json, if it exists)")]))]).optional(), "commands": z.union([z.union([z.intersection(z.string().regex(new RegExp("^\\.\\/.*")), z.string().regex(new RegExp(".*\\.md$"))), z.string().regex(new RegExp("^\\.\\/.*"))]).describe("Path to additional command file or skill directory (in addition to those in the commands/ directory, if it exists), relative to the plugin root"), z.array(z.union([z.intersection(z.string().regex(new RegExp("^\\.\\/.*")), z.string().regex(new RegExp(".*\\.md$"))), z.string().regex(new RegExp("^\\.\\/.*"))]).describe("Path to additional command file or skill directory (in addition to those in the commands/ directory, if it exists), relative to the plugin root")).describe("List of paths to additional command files or skill directories"), z.record(z.string(), z.object({ "source": z.union([z.intersection(z.string().regex(new RegExp("^\\.\\/.*")), z.string().regex(new RegExp(".*\\.md$"))), z.string().regex(new RegExp("^\\.\\/.*"))]).describe("Path to command markdown file, relative to plugin root").optional(), "content": z.string().describe("Inline markdown content for the command").optional(), "description": z.string().describe("Command description override").optional(), "argumentHint": z.string().describe("Hint for command arguments (e.g., \"[file]\")").optional(), "model": z.string().describe("Default model for this command").optional(), "allowedTools": z.array(z.string()).describe("Tools allowed when command runs").optional() })).describe("Object mapping of command names to their metadata and source files. Command name becomes the slash command name (e.g., \"about\" → \"/plugin:about\")")]).optional(), "agents": z.union([z.intersection(z.string().regex(new RegExp("^\\.\\/.*")), z.string().regex(new RegExp(".*\\.md$"))).describe("Path to additional agent file (in addition to those in the agents/ directory, if it exists), relative to the plugin root"), z.array(z.intersection(z.string().regex(new RegExp("^\\.\\/.*")), z.string().regex(new RegExp(".*\\.md$"))).describe("Path to additional agent file (in addition to those in the agents/ directory, if it exists), relative to the plugin root")).describe("List of paths to additional agent files")]).optional(), "skills": z.union([z.string().regex(new RegExp("^\\.\\/.*")).describe("Path to additional skill directory (in addition to those in the skills/ directory, if it exists), relative to the plugin root"), z.array(z.string().regex(new RegExp("^\\.\\/.*")).describe("Path to additional skill directory (in addition to those in the skills/ directory, if it exists), relative to the plugin root")).describe("List of paths to additional skill directories")]).optional(), "outputStyles": z.union([z.string().regex(new RegExp("^\\.\\/.*")).describe("Path to additional output styles directory or file (in addition to those in the output-styles/ directory, if it exists), relative to the plugin root"), z.array(z.string().regex(new RegExp("^\\.\\/.*")).describe("Path to additional output styles directory or file (in addition to those in the output-styles/ directory, if it exists), relative to the plugin root")).describe("List of paths to additional output styles directories or files")]).optional(), "themes": z.union([z.string().regex(new RegExp("^\\.\\/.*")).describe("Path to additional themes directory or file (in addition to those in the themes/ directory, if it exists), relative to the plugin root"), z.array(z.string().regex(new RegExp("^\\.\\/.*")).describe("Path to additional themes directory or file (in addition to those in the themes/ directory, if it exists), relative to the plugin root")).describe("List of paths to additional themes directories or files")]).optional(), "channels": z.array(z.object({ "server": z.string().min(1).describe("Name of the MCP server this channel binds to. Must match a key in this plugin's mcpServers."), "displayName": z.string().describe("Human-readable name shown in the config dialog title (e.g., \"Telegram\"). Defaults to the server name.").optional(), "userConfig": z.record(z.string(), z.object({ "type": z.enum(["string", "number", "boolean", "directory", "file"]).describe("Type of the configuration value"), "title": z.string().describe("Human-readable label shown in the config dialog"), "description": z.string().describe("Help text shown beneath the field in the config dialog"), "required": z.boolean().describe("If true, validation fails when this field is empty").optional(), "default": z.union([z.string(), z.number(), z.boolean(), z.array(z.string())]).describe("Default value used when the user provides nothing").optional(), "multiple": z.boolean().describe("For string type: allow an array of strings").optional(), "sensitive": z.boolean().describe("If true, masks dialog input and stores value in secure storage (keychain/credentials file) instead of settings.json").optional(), "min": z.number().describe("Minimum value (number type only)").optional(), "max": z.number().describe("Maximum value (number type only)").optional() }).strict()).describe("Fields to prompt the user for when enabling this plugin in assistant mode. Saved values are substituted into ${user_config.KEY} references in the mcpServers env.").optional() }).strict()).describe("Channels this plugin provides. Each entry declares an MCP server as a message channel and optionally specifies user configuration to prompt for at enable time.").optional(), "mcpServers": z.union([z.intersection(z.string().regex(new RegExp("^\\.\\/.*")), z.string().regex(new RegExp(".*\\.json$"))).describe("MCP servers to include in the plugin (in addition to those in the .mcp.json file, if it exists)"), z.union([z.string().regex(new RegExp("^\\.\\/.*")).describe("Path to MCPB file relative to plugin root"), z.string().url().describe("URL to MCPB file")]).describe("Path or URL to MCPB file containing MCP server configuration"), z.record(z.string(), z.union([z.object({ "type": z.literal("stdio").optional(), "command": z.string().min(1), "args": z.array(z.string()).default([]), "env": z.record(z.string(), z.string()).optional() }), z.object({ "type": z.literal("sse"), "url": z.string(), "headers": z.record(z.string(), z.string()).optional(), "headersHelper": z.string().optional(), "oauth": z.object({ "clientId": z.string().optional(), "callbackPort": z.number().int().gt(0).lte(9007199254740991).optional(), "authServerMetadataUrl": z.string().url().regex(new RegExp("^https:\\/\\/.*")).optional(), "scopes": z.string().min(1).optional(), "xaa": z.boolean().optional() }).optional() }), z.object({ "type": z.literal("http"), "url": z.string(), "headers": z.record(z.string(), z.string()).optional(), "headersHelper": z.string().optional(), "oauth": z.object({ "clientId": z.string().optional(), "callbackPort": z.number().int().gt(0).lte(9007199254740991).optional(), "authServerMetadataUrl": z.string().url().regex(new RegExp("^https:\\/\\/.*")).optional(), "scopes": z.string().min(1).optional(), "xaa": z.boolean().optional() }).optional() }), z.object({ "type": z.literal("ws"), "url": z.string(), "headers": z.record(z.string(), z.string()).optional(), "headersHelper": z.string().optional() })])).describe("MCP server configurations keyed by server name"), z.array(z.union([z.intersection(z.string().regex(new RegExp("^\\.\\/.*")), z.string().regex(new RegExp(".*\\.json$"))).describe("Path to MCP servers configuration file"), z.union([z.string().regex(new RegExp("^\\.\\/.*")).describe("Path to MCPB file relative to plugin root"), z.string().url().describe("URL to MCPB file")]).describe("Path or URL to MCPB file"), z.record(z.string(), z.union([z.object({ "type": z.literal("stdio").optional(), "command": z.string().min(1), "args": z.array(z.string()).default([]), "env": z.record(z.string(), z.string()).optional() }), z.object({ "type": z.literal("sse"), "url": z.string(), "headers": z.record(z.string(), z.string()).optional(), "headersHelper": z.string().optional(), "oauth": z.object({ "clientId": z.string().optional(), "callbackPort": z.number().int().gt(0).lte(9007199254740991).optional(), "authServerMetadataUrl": z.string().url().regex(new RegExp("^https:\\/\\/.*")).optional(), "scopes": z.string().min(1).optional(), "xaa": z.boolean().optional() }).optional() }), z.object({ "type": z.literal("http"), "url": z.string(), "headers": z.record(z.string(), z.string()).optional(), "headersHelper": z.string().optional(), "oauth": z.object({ "clientId": z.string().optional(), "callbackPort": z.number().int().gt(0).lte(9007199254740991).optional(), "authServerMetadataUrl": z.string().url().regex(new RegExp("^https:\\/\\/.*")).optional(), "scopes": z.string().min(1).optional(), "xaa": z.boolean().optional() }).optional() }), z.object({ "type": z.literal("ws"), "url": z.string(), "headers": z.record(z.string(), z.string()).optional(), "headersHelper": z.string().optional() })])).describe("Inline MCP server configurations")])).describe("Array of MCP server configurations (paths, MCPB files, or inline definitions)")]).optional(), "lspServers": z.union([z.intersection(z.string().regex(new RegExp("^\\.\\/.*")), z.string().regex(new RegExp(".*\\.json$"))).describe("Path to .lsp.json configuration file relative to plugin root"), z.record(z.string(), z.object({ "command": z.string().min(1).describe("Command to execute the LSP server (e.g., \"typescript-language-server\")"), "args": z.array(z.string().min(1)).describe("Command-line arguments to pass to the server").optional(), "extensionToLanguage": z.record(z.string(), z.string().min(1)).describe("Mapping from file extension to LSP language ID. File extensions and languages are derived from this mapping."), "transport": z.enum(["stdio", "socket"]).describe("Communication transport mechanism").default("stdio"), "env": z.record(z.string(), z.string()).describe("Environment variables to set when starting the server").optional(), "initializationOptions": z.any().describe("Initialization options passed to the server during initialization").optional(), "settings": z.any().describe("Settings passed to the server via workspace/didChangeConfiguration").optional(), "workspaceFolder": z.string().describe("Workspace folder path to use for the server").optional(), "startupTimeout": z.number().int().gt(0).lte(9007199254740991).describe("Maximum time to wait for server startup (milliseconds)").optional(), "shutdownTimeout": z.number().int().gt(0).lte(9007199254740991).describe("Maximum time to wait for graceful shutdown (milliseconds)").optional(), "restartOnCrash": z.boolean().describe("Whether to restart the server if it crashes").optional(), "maxRestarts": z.number().int().gte(0).lte(9007199254740991).describe("Maximum number of restart attempts before giving up").optional() }).strict()).describe("LSP server configurations keyed by server name"), z.array(z.union([z.intersection(z.string().regex(new RegExp("^\\.\\/.*")), z.string().regex(new RegExp(".*\\.json$"))).describe("Path to LSP configuration file"), z.record(z.string(), z.object({ "command": z.string().min(1).describe("Command to execute the LSP server (e.g., \"typescript-language-server\")"), "args": z.array(z.string().min(1)).describe("Command-line arguments to pass to the server").optional(), "extensionToLanguage": z.record(z.string(), z.string().min(1)).describe("Mapping from file extension to LSP language ID. File extensions and languages are derived from this mapping."), "transport": z.enum(["stdio", "socket"]).describe("Communication transport mechanism").default("stdio"), "env": z.record(z.string(), z.string()).describe("Environment variables to set when starting the server").optional(), "initializationOptions": z.any().describe("Initialization options passed to the server during initialization").optional(), "settings": z.any().describe("Settings passed to the server via workspace/didChangeConfiguration").optional(), "workspaceFolder": z.string().describe("Workspace folder path to use for the server").optional(), "startupTimeout": z.number().int().gt(0).lte(9007199254740991).describe("Maximum time to wait for server startup (milliseconds)").optional(), "shutdownTimeout": z.number().int().gt(0).lte(9007199254740991).describe("Maximum time to wait for graceful shutdown (milliseconds)").optional(), "restartOnCrash": z.boolean().describe("Whether to restart the server if it crashes").optional(), "maxRestarts": z.number().int().gte(0).lte(9007199254740991).describe("Maximum number of restart attempts before giving up").optional() }).strict()).describe("Inline LSP server configurations")])).describe("Array of LSP server configurations (paths or inline definitions)")]).optional(), "monitors": z.union([z.intersection(z.string().regex(new RegExp("^\\.\\/.*")), z.string().regex(new RegExp(".*\\.json$"))).describe("Path to a JSON file containing the monitors array, relative to the plugin root"), z.array(z.object({ "name": z.string().min(1).describe("Identifier for this monitor, unique within the plugin. Used to dedupe so re-arming (plugin reload, repeat skill invoke) does not spawn duplicates."), "command": z.string().min(1).describe("Shell command to run as a persistent background monitor. Each stdout line is delivered to the model as a <task_notification> event; the process runs for the session lifetime. ${CLAUDE_PLUGIN_ROOT}, ${CLAUDE_PLUGIN_DATA}, ${user_config.*}, and ${ENV_VAR} are substituted. Runs in the session cwd — prefix with `cd \"${CLAUDE_PLUGIN_ROOT}\" && ` if the script needs its own directory."), "description": z.string().min(1).describe("Short human-readable description of what is being monitored (shown in task panel and notification summary)."), "when": z.union([z.literal("always"), z.string().regex(new RegExp("^on-skill-invoke:.*"))]).describe("Arm trigger. \"always\" arms at session start and on plugin reload. \"on-skill-invoke:<skill>\" arms the first time that skill is dispatched (via Skill tool or slash command).").default("always") }).strict())]).describe("Background watch scripts the host arms as persistent Monitor tasks (unsandboxed, same trust tier as hooks) so plugins need not instruct the model to arm them. When omitted, monitors/monitors.json at the plugin root is loaded if present.").optional(), "settings": z.record(z.string(), z.any()).describe("Settings to merge into the user settings while this plugin is enabled. Only the documented allowlisted keys are applied.").optional(), "userConfig": z.record(z.string(), z.object({ "type": z.enum(["string", "number", "boolean", "directory", "file"]).describe("Type of the configuration value"), "title": z.string().describe("Human-readable label shown in the config dialog"), "description": z.string().describe("Help text shown beneath the field in the config dialog"), "required": z.boolean().describe("If true, validation fails when this field is empty").optional(), "default": z.union([z.string(), z.number(), z.boolean(), z.array(z.string())]).describe("Default value used when the user provides nothing").optional(), "multiple": z.boolean().describe("For string type: allow an array of strings").optional(), "sensitive": z.boolean().describe("If true, masks dialog input and stores value in secure storage (keychain/credentials file) instead of settings.json").optional(), "min": z.number().describe("Minimum value (number type only)").optional(), "max": z.number().describe("Maximum value (number type only)").optional() }).strict()).describe("User-configurable values this plugin needs. Prompted at enable time. Non-sensitive values saved to settings.json; sensitive values to secure storage. Available as ${user_config.KEY} in MCP/LSP server config, hook commands, and (non-sensitive only) skill/agent content. Keep sensitive value counts small.").optional(), "source": z.union([z.string().regex(new RegExp("^\\.\\/.*")).describe("Path to the plugin root, relative to the marketplace root (the directory containing .claude-plugin/, not .claude-plugin/ itself)"), z.object({ "source": z.literal("npm"), "package": z.union([z.string(), z.string()]).describe("Package name (or url, or local path, or anything else that can be passed to `npm` as a package)"), "version": z.string().describe("Specific version or version range (e.g., ^1.0.0, ~2.1.0)").optional(), "registry": z.string().url().describe("Custom NPM registry URL (defaults to using system default, likely npmjs.org)").optional() }).describe("NPM package as plugin source"), z.object({ "source": z.literal("url"), "url": z.string().describe("Full git repository URL (https:// or git@)"), "ref": z.string().describe("Git branch or tag to use (e.g., \"main\", \"v1.0.0\"). Defaults to repository default branch.").optional(), "sha": z.string().regex(new RegExp("^[a-f0-9]{40}$")).min(40).max(40).describe("Specific commit SHA to use").optional() }), z.object({ "source": z.literal("github"), "repo": z.string().describe("GitHub repository in owner/repo format"), "ref": z.string().describe("Git branch or tag to use (e.g., \"main\", \"v1.0.0\"). Defaults to repository default branch.").optional(), "sha": z.string().regex(new RegExp("^[a-f0-9]{40}$")).min(40).max(40).describe("Specific commit SHA to use").optional() }), z.object({ "source": z.literal("git-subdir"), "url": z.string().describe("Git repository: GitHub owner/repo shorthand, https://, or git@ URL"), "path": z.string().min(1).describe("Subdirectory within the repo containing the plugin (e.g., \"tools/claude-plugin\"). Cloned sparsely using partial clone (--filter=tree:0) to minimize bandwidth for monorepos."), "ref": z.string().describe("Git branch or tag to use (e.g., \"main\", \"v1.0.0\"). Defaults to repository default branch.").optional(), "sha": z.string().regex(new RegExp("^[a-f0-9]{40}$")).min(40).max(40).describe("Specific commit SHA to use").optional() }).describe("Plugin located in a subdirectory of a larger repository (monorepo). Only the specified subdirectory is materialized; the rest of the repo is not downloaded.")]).describe("Where to fetch the plugin from"), "category": z.string().describe("Category for organizing plugins (e.g., \"productivity\", \"development\")").optional(), "tags": z.array(z.string()).describe("Tags for searchability and discovery").optional(), "strict": z.boolean().describe("Require the plugin manifest to be present in the plugin folder. If false, the marketplace entry provides the manifest.").default(true) })).describe("Collection of available plugins in this marketplace"), "forceRemoveDeletedPlugins": z.boolean().describe("When true, plugins removed from this marketplace will be automatically uninstalled and flagged for users").optional(), "metadata": z.object({ "pluginRoot": z.string().describe("Base path for relative plugin sources").optional(), "version": z.string().describe("Marketplace version").optional(), "description": z.string().describe("Marketplace description").optional() }).describe("Optional marketplace metadata").optional(), "allowCrossMarketplaceDependenciesOn": z.array(z.string()).describe("Marketplace names whose plugins may be auto-installed as dependencies. Only the root marketplace's allowlist applies — no transitive trust.").optional() }).describe("Marketplace manifest (.claude-plugin/marketplace.json) listing Claude Code plugins. Learn more: https://code.claude.com/docs/en/plugin-marketplaces")
